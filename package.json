{
  "name": "readr-server",
  "version": "0.1.0",
  "description": "Save articles and read them later when offline.",
  "scripts": {
    "lint": "eslint ./",
    "spec": "tape ./tests/**/*.spec.js",
    "test": "npm run lint && npm run spec"
  },
  "author": "Emanuel Kluge",
  "license": "Beerware",
  "dependencies": {
    "eslint": "^3.9.1",
    "eslint-config-airbnb": "^13.0.0",
    "eslint-plugin-import": "^2.2.0",
    "eslint-plugin-jsx-a11y": "^2.2.3",
    "eslint-plugin-react": "^6.7.1",
    "firebase-tools": "^3.5.0",
    "tape": "^4.6.3"
  }
}
